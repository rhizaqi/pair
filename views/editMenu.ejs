<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> Edit Menu </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>
<body>
    <nav class="navbar bg-primary" data-bs-theme="dark">
        <div class="container">
            <div class="container-fluid">
                <a class="navbar-brand" href="#"> Add Menu </a>
            </div>
        </div>
        <a class="btn btn-primary" href="/" role="button"> </a> </center>
    </nav>

    <form action="/shop/editMenu/<%= menu.shopId %>/menu/<%= menu.id %>" method="post">
        <p>
            <label> Shop Location </label>
            <select name="location">
                <option value="3" <%= menu.shopId === '3' ? 'selected' : '' %>> Wiyung </option>
                <option value="4" <%= menu.shopId === '4' ? 'selected' : '' %>> Ngagel </option>
            </select>
        </p>
        <p>
            <label> Name </label>
            <input type="text" name="name" value="<%= menu.name %>">
        </p>
        <p>
            <label> Type </label>
            <select name="type">
                <option value="hot" <%= menu.type === 'hot' ? 'selected' : '' %>> Hot </option>
                <option value="ice" <%= menu.type === 'ice' ? 'selected' : '' %>> Ice </option>
                <option value="pastry" <%= menu.type === 'pastry' ? 'selected' : '' %>> Pastry </option>
            </select>
        </p>
        <p>
            <label> Price </label>
            <input type="number" name="price" value="<%= menu.price %>">
        </p>
        <p>
            <label> Image </label>
            <input type="text" name="imageURL" value="<%= menu.imageURL %>">
        </p>
        <input type="submit" value="Add Menu">
    </form>
</body>
</html>